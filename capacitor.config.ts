import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.project2.app',
  appName: 'project2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
